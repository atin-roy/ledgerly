meta {
  name: Login
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email" : "{{email}}",
    "password" : "{{password}}"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
